<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#link-to-animated 'home' class='navbar-brand' animations="main:flip"}}Bodyspace{{/link-to-animated}}
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
          {{ view App.NavitemsTimelinesDropdownView }}
          {{ view App.NavitemsDietsDropdownView }}
        {{else}}
          <li>{{#link-to-animated 'login' animations="main:flip"}}Login{{/link-to-animated}}</li>
          <li>{{#link-to-animated 'registration' animations="main:flip"}}Register{{/link-to-animated}}</li>
        {{/if}}
      </ul>
      {{#if isAuthenticated}}
        <ul class="nav navbar-nav navbar-right">
          {{ view App.NavitemsAccountDropdownView }}
        </ul>
      {{/if}}

    </div>
  </div>
</nav>