<nav class="navbar navbar-inverse navbar-fixed-top">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      {{#linkTo 'home' class='navbar-brand'}}Bodyspace{{/linkTo}}
    </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        {{#if isAuthenticated}}
          <li><a {{action logout}} href="#">Logout</a></li>
          <li class="navbar-text">{{user.name}} </li>
        {{else}}
          <li>{{view App.MenuItem href="#/login" label="Login" }}</li>
          <li>{{view App.MenuItem href="#/registration" label="Register"}}</li>
        {{/if}}
      </ul>
    </div>
  </div>
</nav>